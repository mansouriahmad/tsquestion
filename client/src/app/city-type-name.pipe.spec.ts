import { CityTypeNamePipe } from './city-type-name.pipe';

describe('CityTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CityTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
